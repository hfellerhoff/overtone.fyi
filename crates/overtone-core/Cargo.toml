[package]
name = "overtone-core"
version = "0.1.0"
edition = "2021"
description = "Shared audio analysis core for overtone.fyi (native + wasm)"
license = "MIT"

[dependencies]
realfft = "3.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
